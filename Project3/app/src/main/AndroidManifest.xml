<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project3"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Screen2Activity"
            android:exported="true">
            <!-- Other activity attributes -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Add data elements to specify the data that can trigger this activity -->
                <!-- For example, you can specify a custom scheme or host -->
                <data android:scheme="myapp" android:host="screen2" />
            </intent-filter>
        </activity>

        <activity android:name=".Screen3Activity"
            android:exported="true">
            <!-- Other activity attributes -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Add data elements to specify the data that can trigger this activity -->
                <!-- For example, you can specify a custom scheme or host -->
                <data android:scheme="myapp" android:host="screen3" />
            </intent-filter>
        </activity>

    </application>

</manifest>